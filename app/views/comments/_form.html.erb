
<% if @comment.errors.any? %>
  <%= error_helper(@comment) %>
  <%= error_list(@comment) %>
<% end %>

<%= form_for [@show, @comment] do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :body %>
  <%= f.submit %>
<% end%>
